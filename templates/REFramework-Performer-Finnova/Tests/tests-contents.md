# Tests.xlsx contents

<!-- GENERATED by templates/tools/build-template-workbooks.ps1 — do not edit by hand. -->

Driven by Tests\RunAllTests.xaml. Every row below is a placeholder — the suite does not
pass as shipped and is not meant to. Replace all three rows with real workflows before UAT;
an unreplaced `[UC-SPECIFIC — replace]` row is a review finding.

## `Tests` sheet

| WorkflowFile | ExpectedResult |
|---|---|
| [UC-SPECIFIC — replace] — e.g. Framework\InitAllSettings.xaml | Success |
| [UC-SPECIFIC — replace] — e.g. Tests\Test-&lt;Application&gt;_Login.xaml | Success |
| [UC-SPECIFIC — replace] — e.g. Tests\Test-&lt;Application&gt;_Login_BadCredentials.xaml | SystemException |

## `Result` sheet

| WorkflowFile | ExpectedResult | Status | Comments |
|---|---|---|---|

Header row only — the rows are written by the test run.

